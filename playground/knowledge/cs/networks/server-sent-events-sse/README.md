# Server-Sent Events (SSE)

_Planned topic — no material here yet._

Intended for notes and experiments on SSE: the `text/event-stream` format, one-way server → client
streaming, and auto-reconnect. See the [networks README](../README.md).
