# Go

_Planned language — no material here yet._

Add a `projects/` bucket for coherent builds or a `practice/` bucket for loose drills as content
arrives (mirroring the [c](../c/) and [rust](../rust/) layouts). See the
[programming-languages README](../../README.md).
